CREATE OR REPLACE FUNCTION FUN_ROW_NOME_TABELA
(P_NOME_PK   NOME_TABELA.NOME_PK%TYPE) 
RETURN NOME_TABELA%ROWTYPE IS
CURSOR CUR_NOME_TABELA IS
SELECT * FROM NOME_TABELA 
WHERE NOME_PK = P_NOME_PK;
BEGIN
  FOR V_ROW IN CUR_NOME_TABELA LOOP
     RETURN V_ROW;
  END LOOP;
  RETURN NULL;
END;

--NOME_TABELA
--NOME_PK
--NOME_COLUNA
SET SERVEROUTPUT ON;
DECLARE
V_ROW  NOME_TABELA%ROWTYPE;  
BEGIN
  V_ROW := FUN_NOME_TABELA(&VALOR_PK);
  DBMS_OUTPUT.PUT_LINE(V_ROW.NOME_COLUNA);
END;

